export const Gender = Object.freeze({
  MALE: 'male',
  FEMALE: 'female',
  NON_BINARY: 'non_binary',
});

export const Citizenship = Object.freeze({
  CITIZEN: 'citizen',
  RESIDENT: 'resident',
  FOREIGN: 'foreign',
});
